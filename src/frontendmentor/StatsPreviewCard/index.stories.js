import StatsPreviewCard from "./index"

export default {
  name: "StatsPreviewCard",
  component: StatsPreviewCard,
}

export const Default = (args) => <StatsPreviewCard {...args} />
Default.args = {}
